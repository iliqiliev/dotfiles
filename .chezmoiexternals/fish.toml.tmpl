{{-
  $fishVersion := joinPath
    .chezmoi.sourceDir "private_dot_local/bin/executable_fish_version"
}}
{{-
  if or
    (ne .os "linux")
    (not (lookPath "bash"))
    (exec "bash" $fishVersion "4" "0" "0")
}}
{{-   abortEmpty }}
{{- end }}

{{- $arch := output "uname" "--machine" | trim }}
{{- $fish := printf "fish-*-linux-%s.tar.xz" $arch -}}

[".local/bin/fish"]
type = "archive-file"
path = "fish"
executable = true
url = {{ gitHubLatestReleaseAssetURL "fish-shell/fish-shell" $fish | quote }}
refreshPeriod = "672h"
