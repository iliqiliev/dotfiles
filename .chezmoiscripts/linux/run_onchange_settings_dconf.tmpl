#!{{ lookPath "bash" }}

# dconf.ini hash: {{ include ".data/dconf.ini" | sha256sum }}

if [ ! -x "$(command -v dconf)" ]; then
	exit 0
fi

dconf load / < {{ joinPath .chezmoi.sourceDir ".data/dconf.ini" }}
