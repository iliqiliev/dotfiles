<% if echo "$YADM_USER $YADM_CLASSES" | grep -Eiqw "iliq|iliya"; then -%>
[user]
    name = Iliya Iliev
    email = iliq0000@proton.me
    username = iliqiliev

<% fi -%>
[core]
    autocrlf = input
<% if command -v delta &> /dev/null; then -%>
    pager = delta

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true

<% fi -%>
[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default

[credential "https://github.com"]
	helper =
	helper = !gh auth git-credential

[credential "https://gist.github.com"]
	helper =
	helper = !gh auth git-credential
