# These are the fish abbreviations.
{{ include "private_dot_config/fish/conf.d/30_abbreviations.fish" -}}

# These are normally built-in into fish so they are defined here.
abbr .. "cd .."
abbr ll "ls -l"
abbr la "ls -la"
